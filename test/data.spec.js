require("../src/data.js");

describe("filtro", () => {
  it("é uma função", () => {
    expect(typeof data.filtro).toBe("function");
  });

  // it("returns `example`", () => {
  //   expect(example()).toBe("example");
  // });
});
